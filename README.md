# 🛠️ API Soft Tools

Este repositório contém a **API SoftTools**, responsável pelo gerenciamento do banco de dados do sistema Soft Tools. A API serve como ponte entre o front-end e o banco de dados, permitindo operações como a criação, consulta, atualização e remoção de dados, dentre outras funcionalidades.

## 📦 Sobre o Projeto

A **API SoftTools** centraliza toda a lógica de acesso e manipulação de dados da aplicação. É aqui que estão definidas as rotas, os modelos das tabelas e a conexão com o banco de dados utilizado.

## ⚙️ Tecnologias Utilizadas
 
- Python 3.8+
- FastAPI
- Banco de dados: PostgreSQL ou MySQL

## 🚀 Como Executar

1. Clone este repositório:
   ```bash
   git clone https://github.com/salvinoamanda/API-Softtools.git
   ```

2. Acesse o diretório do projeto:
   ```bash
   cd API-Softtools
   ```

3. Instale as dependências:
   ```bash
   - fastapi
   - uvicorn[standard]
   - python-dotenv
   ```

## 📌 Observações

- Este repositório é dedicado apenas ao **back-end** da aplicação.

# Comando para rodar o servidor 
uvicorn src.myapp.main:app --reload

